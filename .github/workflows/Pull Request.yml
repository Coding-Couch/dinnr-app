name: Build

on:
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2.4.0
    - name: Build and Test
      run: cd Dinnr \ 
        xcodebuild test -scheme Dinnr -destination='os=iOS Simulator,name=iPhone 13 Pro Max'
